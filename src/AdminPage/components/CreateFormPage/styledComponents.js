import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const CreateFormContainer = styled.div`
   ${tw`flex flex-col bg-white-100`}
   max-height:100vh;
   background-color: #f1f7ff;
`
export const CreatePageBody = styled.div`
   ${tw`flex`}
`
